{
  "name": "coliseum-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-driven live tests. Each context carries its own injected wallet, so several players can play at once.",
  "scripts": {
    "test": "playwright test",
    "headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "viem": "~2.37.8"
  }
}
